name: Test ArgoCD CLI invalid version error handling
on:
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2

      - name: Run action with invalid version string
        id: action
        uses: .
        with:
          version: not-a-valid-version
        continue-on-error: true

      - name: Assert action failed with invalid version
        env:
          OUTCOME: ${{ steps.action.outcome }}
        run: |
          echo "outcome: $OUTCOME"
          if [ "$OUTCOME" != "failure" ]; then
            echo "FAIL: expected action to fail with invalid version, got outcome: $OUTCOME"
            exit 1
          fi
          echo "PASS: action correctly failed with invalid version string"
